replicas: 1

resources:
  requests:
    cpu: 400m
    memory: 128Mi
  limits:
    cpu: 700m
    memory: 256Mi

strategy:
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0

keys:
  token: 'STATIC_API_TOKEN' # TODO: сервисный токен для сервиса Static API
