{{- if .Values.head.vpa.enabled }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "search-api-v8.fullname" . }}-head
  labels:
    {{- include "search-api-v8.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "search-api-v8.fullname" . }}-head
  updatePolicy:
    updateMode: {{ ternary "On" "Off" .Values.head.vpa.updateMode | quote }}
{{- end }}
