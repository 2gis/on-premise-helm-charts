{{- if ne (include "license.type" $) "1" -}}

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "license.fullname" $ }}
rules:
  - apiGroups: [""]
    resources:
      - pods
    verbs:
      - get
      - list
      - watch

{{- end -}}
