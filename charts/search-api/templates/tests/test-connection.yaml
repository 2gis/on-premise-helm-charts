apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "big-search.fullname" . }}-test-connection"
  labels:
    {{- include "big-search.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "big-search.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
