{{- if .Values.service.ingressRouter.external.enable }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "search-api.fullname" . }}-external
spec:
  hostnames:
  - {{ .Values.service.ingressRouter.external.host }}
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: shared
    namespace: istio-gateways
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: canary
    namespace: istio-gateways
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: stable
    namespace: istio-gateways
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: {{ include "search-api.fullname" . }}-ingress-router
      port: {{ .Values.service.ingressRouter.port }}
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /
{{- end }}
