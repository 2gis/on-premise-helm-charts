{{- if .Values.geodetector.enabled }}
{{- if .Values.geodetector.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "big-search.fullname" . }}-geodetector
  labels:
    {{- include "big-search.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "big-search.fullname" . }}-geodetector
  minReplicas: {{ .Values.geodetector.hpa.minReplicas }}
  maxReplicas: {{ .Values.geodetector.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: AverageValue
          averageValue: {{ .Values.geodetector.hpa.averageCPUValue }}
  {{- if .Values.geodetector.hpa.behavior }}
  behavior:
    {{- .Values.geodetector.hpa.behavior | toYaml | nindent 4 }}
  {{- end }}{{- /* .Values.geodetector.hpa.behavior */}}
{{- end }}{{- /* .Values.geodetector.hpa.enabled */}}
{{ end }}{{- /* .Values.geodetector.enabled */}}