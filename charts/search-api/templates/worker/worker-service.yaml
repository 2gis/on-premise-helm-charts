apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-api.fullname" . }}-worker
  labels:
    {{- include "search-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.worker.port }}
      targetPort: {{ .Values.worker.ports.grpc }}
      protocol: TCP
      name: grpc
  selector:
    {{- include "search-api.selectorLabels" . | nindent 4 }}
    search/part: worker
