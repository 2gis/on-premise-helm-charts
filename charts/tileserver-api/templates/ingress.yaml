{{- if .Values.tileserver.api.ingress.enabled -}}
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tileserver.name" . }}
  labels:
    {{- include "api.labels" . | nindent 4 }}
  {{- with .Values.tileserver.api.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.tileserver.api.ingress.className }}
  {{- if .Values.tileserver.api.ingress.tls }}
  tls:
    {{- range .Values.tileserver.api.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.tileserver.api.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "tileserver.name" $ }}
                port:
                  number: {{ $.Values.tileserver.api.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
