{{- define "merger.deployment" }}
metadata:
  name: {{ include "generic-chart.fullname" . }}-merger
spec:
  replicas: {{ .Values.dm.merger.replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "generic-chart.name" . }}-merger
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      annotations:
        checksum/secret: {{ (include (print $.Template.BasePath "/secret.yaml") . | fromYaml).data | toYaml | sha256sum }}
        {{- if .Values.customCAs.bundle }}
        checksum/custom-ca: {{ (include (print $.Template.BasePath "/configmap-ca.yaml") . | fromYaml).data | toYaml | sha256sum }}
        {{- end }}
      labels:
        {{- include "navi-async-matrix.mergerlabels" . | nindent 8 }}
      {{- if and .Values.dm.merger.pdb.enabled .Values.dm.merger.pdb.component }}
        app.kubernetes.io/component: {{ .Values.dm.merger.pdb.component }}
      {{- end }}
    spec:
      volumes:
      {{- $kafkaVolumeMounted := (
              .Values.kafka.fileProperties |
              or (.Values.kafka.consumerOverrides).fileProperties |
              or (.Values.kafka.producerOverrides).fileProperties
            ) }}
      {{- if $kafkaVolumeMounted }}
        - name: {{ printf "%s-consumer" (include "generic-chart.fullname" .) | quote }}
          secret:
            secretName: {{ printf "%s-consumer" (include "generic-chart.fullname" .) | quote }}
        - name: {{ printf "%s-producer" (include "generic-chart.fullname" .) | quote }}
          secret:
            secretName: {{ printf "%s-producer" (include "generic-chart.fullname" .) | quote }}
      {{- end }}
      {{- if .Values.db.tls.enabled }}
        - name: {{ include "navi-async-matrix.fullname-psql-raw" . | quote }}
          secret:
            secretName: {{ include "navi-async-matrix.fullname-psql" . | quote }}
        - name: {{ include "navi-async-matrix.fullname-psql" . | quote }}
          emptyDir: {}
      {{- end }}
      {{- if .Values.customCAs.bundle }}
        - name: {{ include "generic-chart.fullname" . }}-ca
          configMap:
            name: {{ include "generic-chart.fullname" . }}-ca
      {{- end }}
      # TODO: SA name from generic-chart
      serviceAccountName: {{ include "navi-async-matrix.serviceAccountName" . }}
      initContainers:
        {{- if .Values.db.tls.enabled }}
        - name: copy-certs
          image: '{{ required "A valid .Values.dgctlDockerRegistry entry required" .Values.dgctlDockerRegistry }}/{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}'
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command:
            - /bin/sh
            - -ec
            - |
              cp /tmp/certs/* {{ printf "%s/secret/psql/" .Values.dm.configFilepath | quote }}
              chmod 600 {{ printf "%s/secret/psql/client.key" .Values.dm.configFilepath | quote }}
          volumeMounts:
            - name: {{ include "navi-async-matrix.fullname-psql-raw" . | quote }}
              mountPath: /tmp/certs
            - name: {{ include "navi-async-matrix.fullname-psql" . | quote }}
              mountPath: {{ printf "%s/secret/psql" .Values.dm.configFilepath | quote }}
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}-merger
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ required "A valid .Values.dgctlDockerRegistry entry required" .Values.dgctlDockerRegistry }}/{{ .Values.dm.merger.image.repository }}:{{ .Values.dm.merger.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          livenessProbe:
            {{- if .Values.dm.merger.customLivenessProbe }}
            {{- include "tplvalues.render" (dict "value" .Values.dm.merger.customLivenessProbe "context" $) | nindent 12 }}
            {{- else if .Values.dm.merger.livenessProbe.enabled }}
            httpGet:
              path: /healthcheck
              port: 8003
            initialDelaySeconds: {{ .Values.dm.merger.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.dm.merger.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.dm.merger.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.dm.merger.livenessProbe.failureThreshold }}
            {{- end }}
          readinessProbe:
            {{- if .Values.dm.merger.customReadinessProbe }}
            {{- include "tplvalues.render" (dict "value" .Values.dm.merger.customReadinessProbe "context" $) | nindent 12 }}
            {{- else if .Values.dm.merger.readinessProbe.enabled }}
            httpGet:
              path: /healthcheck
              port: 8003
            initialDelaySeconds: {{ .Values.dm.merger.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.dm.merger.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.dm.merger.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.dm.merger.readinessProbe.failureThreshold }}
            {{- end }}
          resources:
            {{- toYaml .Values.dm.merger.resources | nindent 12 }}
          env:
            - name: CONFIG_TYPE
              value: env
            - name: LOGGER_SETTING__level
              value: {{ .Values.dm.logLevel | quote }}
            - name: LOGGER_SETTING__FORMAT
              value: {{ .Values.dm.logFormat | quote }}
            - name: LOGGER_SETTING__VERBOSE
              value: "true"
            {{- if .Values.sentry.enabled }}
            - name: SENTRY__DSN
              value: {{ .Values.sentry.address | quote }}
            - name: SENTRY__ENVIRONMENT
              value: {{ .Values.sentry.environment | quote }}
            {{- end }}
            - name: S3_ENDPOINT_URL
              value: {{ required "A valid s3.host entry required" .Values.s3.host | quote }}
            - name: S3_BUCKET
              value: {{ required "A valid s3.bucket entry required" .Values.s3.bucket | quote }}
            - name: S3_REGION_NAME
              value: {{ .Values.s3.region | quote }}
            - name: S3_ACCESS_KEY
              value: {{ required "A valid s3.accessKey entry required" .Values.s3.accessKey | quote }}
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "generic-chart.fullname" . | quote }}
                  key: s3key
            - name: DATABASE__SCHEMA_NAME
              value: {{ .Values.db.schema | quote }}
            - name: DATABASE__DSN
              valueFrom:
                secretKeyRef:
                  name: {{ include "generic-chart.fullname" . | quote }}
                  key: dbDsn
            - name: KAFKA_PRODUCER_SETTING__TOPIC
              value: {{ required "A valid kafka.mergerStatusTopic entry required" .Values.kafka.mergerStatusTopic | quote }}
            - name: KAFKA_CONSUMER_SETTING__TOPIC
              value: {{ required "A valid kafka.mergerTaskTopic entry required" .Values.kafka.mergerTaskTopic | quote }}
            - name: KAFKA_CONSUMER_SETTING__CONFIG__GROUP_ID
              value: {{ .Values.kafka.mergerGroupId | quote }}
              {{- range $kafkaRole, $overridesProperty := (dict
                  "PRODUCER" "producerOverrides"
                  "CONSUMER" "consumerOverrides"
                ) -}}
              {{- $kafkaPropertyEnv := print "KAFKA_" $kafkaRole "_SETTING__CONFIG__" -}}
              {{- $kafkaPropertiesEnv := include "navi-async-matrix.kafkaPropertiesEnv" (dict
                    "kafka" $.Values.kafka
                    "overrides" ($overridesProperty | get $.Values.kafka)
                    "secretname" (print
                        (include "generic-chart.fullname" $)
                        "-"
                        (lower $kafkaRole)
                      )
                    "mountpoint" (printf "%s/secret/%s" $.Values.dm.configFilepath (lower $kafkaRole))
                    "prefix" $kafkaPropertyEnv
                  ) | fromYaml }}
              {{- get $kafkaPropertiesEnv "ret" | toYaml | nindent 12 }}
              {{- end }}{{- /* range */}}
          volumeMounts:
          {{- if $kafkaVolumeMounted }}
          - name: {{ printf "%s-consumer" (include "generic-chart.fullname" .) | quote }}
            mountPath: {{ printf "%s/secret/consumer/" $.Values.dm.configFilepath | quote }}
          - name: {{ printf "%s-producer" (include "generic-chart.fullname" .) | quote }}
            mountPath: {{ printf "%s/secret/producer/" $.Values.dm.configFilepath | quote }}
          {{- end }}
          {{- if .Values.db.tls.enabled }}
          - name: {{ printf "%s-psql" (include "generic-chart.fullname" .) | quote }}
            mountPath: {{ printf "%s/secret/psql" .Values.dm.configFilepath | quote }}
          {{- end }}
          {{- if .Values.customCAs.bundle }}
          - name: {{ include "generic-chart.fullname" . }}-ca
            mountPath: {{ include "custom.ca.mountPath" . }}/custom-ca.crt
            subPath: custom-ca.crt
          {{- end }}
{{- end }}{{- /* define "merger.deployment" */}}
{{- template "generic-chart.merge" (list . "merger.deployment" "generic-chart.deployment.tpl") }}
