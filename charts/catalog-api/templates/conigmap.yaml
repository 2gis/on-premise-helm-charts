{{- if .Values.customCAs }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "catalog.name" . }}-configmap
data:
  custom-ca.crt: |-
{{ print .Values.customCAs.bundle | indent 4 }}
{{- end }}
