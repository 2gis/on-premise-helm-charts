{{- if empty .Values..api.serviceAccountOverride }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pro-api.service-account-name" . }}
{{- else }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values..api.serviceAccountOverride }}
{{- end }}
