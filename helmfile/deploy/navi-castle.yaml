---
bases:
- ../base.yaml
---
releases:
  - name: navi-castle
    chart: ../../charts/navi-castle
    namespace: {{ .Values.namespace  }}
    kubeContext: {{ .Values.kubeContext }}
    version: "{{ .Values.version }}"
    timeout: 1200
    labels:
      service: navi-castle
    values:
      - ../values/dgctl.yaml
      - ../values/navi-castle.yaml
